Para ejecutar el programa se debe compilar con el comando make en la terminal, por lo que es necesario tener este instalado (sudo apt install make).
Esto genera el archivo compilador que se puede ejecutar con ./compilador
Probado y funcionando correctamente en ubuntu20.4 con c99.
